// /team/js/config.js
// Shibam Coffee Atlanta — employee portal configuration
// -----------------------------------------------------------------------------
// Cloudflare Pages Functions handles login, existing portal forms, and
// scheduling at a same-origin endpoint. D1 is bound server-side as TEAM_DB.
// -----------------------------------------------------------------------------

const CONFIG = {
  API_URL: "/api/team",
  STORE_NAME: "Shibam Coffee Atlanta",
  TURNSTILE_SITEKEY: ""
};
